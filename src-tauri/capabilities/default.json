{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability grants core invoke access to the main window.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
